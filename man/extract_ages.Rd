% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bacon-output.R
\name{extract_ages}
\alias{extract_ages}
\title{Extract Event Ages from Bacon Output Files}
\usage{
extract_ages(
  folder_path,
  record_data,
  event_types,
  max_depths,
  isochrons,
  test_horizons,
  synced = "",
  reload_existing = FALSE,
  excluded_depths = NULL,
  thick = NULL
)
}
\arguments{
\item{folder_path}{Character string giving the parent directory that contains
per-record Bacon output sub-folders.}

\item{record_data}{Named list of data frames representing each record's
metadata (output from \code{load_excel_data()}).}

\item{event_types}{Character vector of all event type names present in your
dataset.}

\item{max_depths}{Named numeric vector of maximum depths per record.}

\item{isochrons}{Character vector of deposit names known to be synchronous.}

\item{test_horizons}{Character vector of event deposits for which
synchronicity should be tested.}

\item{synced}{Character string suffix identifying synchronized output folders
(default: \code{""}).}

\item{reload_existing}{Logical; when \code{TRUE} reads from an already-exported
Excel file instead of re-processing \code{.out} files (default: \code{FALSE}).}

\item{excluded_depths}{Optional named list of excluded depth intervals per record.}

\item{thick}{The Bacon thickness parameter used during age-depth modelling;
single numeric, named list per record, or \code{NULL} to extract from the
folder name (default: \code{NULL}).}
}
\value{
Named list of data frames, one per processed record, containing depth
columns plus one column per event with interpolated ages.
}
\description{
I/O wrapper that reads Bacon \code{.out} files and returns a named list of
processed event ages. Combines \code{read_bacon_output()} and
\code{extract_event_ages()} into a single call. When \code{reload_existing}
is \code{TRUE} the previously exported Excel file is returned instead.
}
