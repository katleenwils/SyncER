% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synchronicity-helpers.R
\name{compare_pair}
\alias{compare_pair}
\title{Compute a log-ratio values for a single pair-wise comparison}
\usage{
compare_pair(
  col1,
  col2,
  rec_i,
  rec_j,
  var_i = NULL,
  var_j = NULL,
  horizon,
  thresholds,
  summaries,
  n_samples = 10000
)
}
\arguments{
\item{col1}{Numeric vector of posterior age samples for record \code{rec_i}.}

\item{col2}{Numeric vector of posterior age samples for record \code{rec_j}.}

\item{rec_i}{Character string identifying record i.}

\item{rec_j}{Character string identifying record j.}

\item{var_i}{Column name used from record i. Pass \code{NULL} (default) for single-horizon
comparisons; \code{variant1} in the stats row is then set to \code{NA}.}

\item{var_j}{Column name used from record j. Same semantics as \code{var_i}.}

\item{horizon}{Character string identifying the event/horizon being compared.}

\item{thresholds}{Named list as returned by \code{get_horizon_thresholds()}.}

\item{summaries}{Named list of per-record summary data frames from
\code{process_event_ages()$summaries} (for min/max in the viz row).}

\item{n_samples}{Integer; number of Monte Carlo samples (default: 10000).}
}
\value{
Named list with elements \code{lr_name}, \code{variant_key}, \code{ABlr},
\code{score}, \code{precision}, \code{stats_row}, \code{viz_row}.
}
\description{
Core computation for one record pair within \code{compute_synchronicity_values()}.
Draws \code{n_samples} log-ratios, scores them against the age-difference bounds,
assesses distribution shape, estimates precision, and assembles the statistics and
visualization rows.
}
\keyword{internal}
