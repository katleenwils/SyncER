% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age-input.R
\name{write_age_input_data}
\alias{write_age_input_data}
\title{Write a build_age_input() result to a folder}
\usage{
write_age_input_data(age_input, out_key, base_dir)
}
\arguments{
\item{age_input}{List with a \code{c14} data frame and optional \code{pb210}
data frame, as returned by \code{build_age_input()}.}

\item{out_key}{Character key naming the output folder and file stem.}

\item{base_dir}{Character path the \code{out_key} folder is created under.}
}
\value{
Invisibly \code{NULL}; called for its side effect of writing CSV files.
}
\description{
Writes the radiocarbon CSV (and, when present, the Pb210 CSV) of a
\code{build_age_input()} result into \code{base_dir/out_key}, creating the
folder if needed. Follows the \emph{rbacon}/\emph{rplum} naming convention:
\code{<key>_C14.csv} plus \code{<key>.csv} when lead data is present, otherwise
\code{<key>.csv} for the radiocarbon data.
}
\keyword{internal}
