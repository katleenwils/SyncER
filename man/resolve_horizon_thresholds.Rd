% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synchronicity-helpers.R
\name{resolve_horizon_thresholds}
\alias{resolve_horizon_thresholds}
\title{Resolve Per-Horizon Confidence Level, Age Difference, and Log Bounds}
\usage{
resolve_horizon_thresholds(
  horizon,
  confidence_level,
  age_difference,
  summaries
)
}
\arguments{
\item{horizon}{Character string identifying the horizon (key for named lookups).}

\item{confidence_level}{Numeric scalar or named numeric vector.}

\item{age_difference}{Numeric scalar or named numeric vector.}

\item{summaries}{Named list of per-record summary data frames from
\code{process_event_ages()$summaries}. Used only when \code{age_difference >= 1}
to derive a relative proportion from the mean age.}
}
\value{
Named list with:
\itemize{
\item \code{conf_level_h}: Effective confidence level.
\item \code{age_diff_h}: Effective (relative) age difference.
\item \code{age_diff_log_bounds}: Two-element numeric \code{c(-t, t)} where
\code{t = log(1 + age_diff_h)}.
}
}
\description{
Extracts \code{conf_level_h} and \code{age_diff_h} for a given horizon from
scalar or named-vector inputs, converts an absolute age difference (>= 1) to a
relative one using the mean age from \code{summaries}, and computes symmetric
log-space bounds.
}
\keyword{internal}
