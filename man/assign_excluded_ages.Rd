% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-synchronized-ages.R
\name{assign_excluded_ages}
\alias{assign_excluded_ages}
\title{Assign Ages to Excluded Horizons}
\usage{
assign_excluded_ages(adjusted_ages, excluded_horizons, event_stats)
}
\arguments{
\item{adjusted_ages}{Output from \code{set_to_zero()} containing synchronized ages.}

\item{excluded_horizons}{Named or unnamed character vector specifying excluded horizons
(same format as in \code{set_to_zero()}).}

\item{event_stats}{Output from \code{process_event_ages()} containing record metadata.}
}
\value{
Named list with one element per excluded horizon, each containing a data frame
with columns: \code{record}, \code{adjusted_age}, \code{adjusted_error}.
}
\description{
Assigns synchronized ages to horizons that were excluded from the main synchronization process.
}
\details{
For each excluded horizon, assigns the age and error from the first available
non-excluded horizon in the same record. Parses exclusions using the same logic as
\code{set_to_zero()} (named for per-record, unnamed for global).
}
