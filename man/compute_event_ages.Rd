% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age-output.R
\name{compute_event_ages}
\alias{compute_event_ages}
\title{Interpolate event ages from MCMC runs of Raw Bacon/Plum output files}
\usage{
compute_event_ages(
  raw_out_data,
  record_data,
  event_types,
  max_depths,
  isochrons,
  test_horizons,
  instantaneous_event_depths = NULL,
  thick = NULL,
  synced = ""
)
}
\arguments{
\item{raw_out_data}{Named list of character vectors (raw lines from each .out file),
one element per record folder.}

\item{record_data}{Named list of data frames representing each record's metadata
(output from \code{read_record_data()}).}

\item{event_types}{Character vector of all event type names present in your dataset.}

\item{max_depths}{Named vector containing the depths to which the age models are calculated for each record.}

\item{isochrons}{Character vector of deposit names that are known to be synchronous.}

\item{test_horizons}{Character vector of event deposits for which synchronicity should be tested.}

\item{instantaneous_event_depths}{Optional named list of depth intervals classified as instantaneous deposits per record
that should not be considered for event-free depths.}

\item{thick}{The \emph{rbacon}/\emph{rplum} section thickness; single numeric, named list per record, or NULL
(default), in which case it is derived per record from the model as
\code{max_depths[record] / (n_cols - 3)}, i.e. the modelled depth range divided
by the number of \emph{rbacon}/\emph{rplum} sections.}

\item{synced}{Character string suffix to identify synchronized folders (default: "").}
}
\value{
Named list of data frames, one per processed record folder, containing
depth columns plus one column per event with interpolated ages.
}
\description{
Performs all age interpolation and computation logic from raw \emph{rbacon}/\emph{rplum} .out file content.
}
