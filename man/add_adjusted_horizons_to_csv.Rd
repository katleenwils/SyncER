% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-adjusted-horizons.R
\name{add_adjusted_horizons_to_csv}
\alias{add_adjusted_horizons_to_csv}
\title{Add Adjusted Horizons to Bacon CSV Files}
\usage{
add_adjusted_horizons_to_csv(
  parent_folder,
  record_data,
  adjusted_ages,
  non_synchro = FALSE,
  overwrite = FALSE,
  synced = FALSE,
  update_records = "_synced",
  age_record = "_synced"
)
}
\arguments{
\item{parent_folder}{Character string specifying the root directory containing Bacon
input folders.}

\item{record_data}{Named list of record metadata (output from \code{load_excel_data()}).}

\item{adjusted_ages}{Output from \code{set_to_zero()} containing synchronized ages.}

\item{non_synchro}{Logical indicating whether to print detailed information about added
samples (default: FALSE).}

\item{overwrite}{Logical indicating whether to overwrite existing files (TRUE) or create
new folders with _synced suffix (FALSE, default).}

\item{synced}{Logical indicating whether input folders already have _synced suffix (TRUE)
or are original folders (FALSE, default).}

\item{update_records}{Character string suffix identifying folders to update when
\code{synced = TRUE} (default: "_synced").}

\item{age_record}{Character string suffix to strip from record names in \code{adjusted_ages}
for matching (default: "_synced").}
}
\value{
No return value. Modifies or creates CSV files and prints progress messages.
}
\description{
Adds synchronized horizon ages to existing Bacon input CSV files, creating updated
versions for re-running age-depth models.
}
\details{
When \code{synced = FALSE} (default):
\itemize{
\item Searches for folders matching record names from \code{record_data}
\item If \code{overwrite = TRUE}: modifies CSV files in original folders
\item If \code{overwrite = FALSE}: creates new folders with "_synced" suffix
}

When \code{synced = TRUE}:
\itemize{
\item Searches for folders with \code{update_records} suffix
\item If \code{overwrite = TRUE}: modifies CSV files in _synced folders
\item If \code{overwrite = FALSE}: creates new folders with incremental numeric suffixes
(e.g., "core1_synced_1", "core1_synced_2")
}
}
